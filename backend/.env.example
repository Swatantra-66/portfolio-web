# Database Configuration
# For local development, use individual DB variables:
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=portfolio
DB_SSLMODE=disable

# OR use DATABASE_URL for production (Render.com format):
# DATABASE_URL=postgres://username:password@host:port/database

# Server Configuration
PORT=8080
GIN_MODE=release

# Admin Secret (change this to a strong random string!)
ADMIN_SECRET=your-super-secret-key-change-this

# Frontend URL (for CORS)
FRONTEND_URL=https://your-frontend-domain.com
